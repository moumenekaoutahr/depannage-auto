framework:
    secret: '%env(APP_SECRET)%'
    trusted_hosts:
        - 'localhost'
        - '127.0.0.1'
        - 'localhost:8000'
        - '127.0.0.1:8000'
    session: true

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file